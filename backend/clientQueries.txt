// Query to get all the element in the user array
Query {
    users{
        id, 
        name,
        surname,
        class
    }
}

//Query to get the whole quotes array
 Query {
    quotes{
        id,
        quote
    }
 }

 //Query to map all the User with thier quote
 Query{
    users{
        id, 
        name,
        surname,
        class,
        quotes{
            quote
        }
    }
 }

 //Query to get the details of the individual user details
 Query{
    user(id: $id){
        id,
        name, 
        surname,
        class
    }
 }

 //Query to get the detail of the individual quote detail
 Query{
    iquote(id:$id){
        id,
        quote
    }
 }

//Query Params
Query getByQuoteId ($userId:ID!){
    iquote(id:$id) {
        id,
        quote
    }
}
query Params
{
    $userId:1
}

//Query to the creation of a user
mutation createUser($userNew:userInput!){
  signUpUserDummy(userNew:$userNew){
    id,
    name,
    surname,
    password
    
  }
}
